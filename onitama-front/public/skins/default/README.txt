Adicione aqui as imagens da skin default:
- default_mestre.png
- default_peao.png

Estas serão usadas quando o usuário tiver skin ativa com `imagem = default` ou quando não houver skin definida (fallback).